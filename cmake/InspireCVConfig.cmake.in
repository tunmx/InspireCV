@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@INSPIRECV_PACKAGE_NEEDS_OPENCV@)
    find_dependency(OpenCV)
endif()

if(@INSPIRECV_PACKAGE_NEEDS_CUDA@)
    find_dependency(CUDAToolkit)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/InspireCVTargets.cmake")
check_required_components(InspireCV)
